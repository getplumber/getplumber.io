---
title: Compliance Controls
description: Plumber's compliance controls for GitLab CI/CD and GitHub Actions. Each control raises a documented issue when violated.
sidebar:
  order: 1
  label: Controls
---

import ProviderTabsScript from "@/docs/components/mdx-components/ProviderTabsScript.astro";

Plumber checks your CI/CD configuration and project settings against a catalog of controls. The catalog is split per provider: GitLab controls inspect `.gitlab-ci.yml` and GitLab project settings, GitHub controls inspect `.github/workflows/*.yml` and GitHub repository settings. Pick the provider tab below to see the relevant catalog.

When a control is not respected, an issue is created. Click the `ISSUE-XXX` link in any row to see the full description, impact, and remediation for that issue.

<div class="not-content my-4 rounded-md border border-border bg-muted/30 px-3 py-2.5 text-[13px] leading-snug text-muted-foreground">
  <p class="mb-2 font-medium text-foreground">Scope</p>
  <ul class="m-0 list-none space-y-1 pl-0 [&>li]:pl-0">
    <li><strong class="text-foreground">All</strong>: Plumber Platform and Open Source CLI</li>
    <li><strong class="text-foreground">Platform</strong>: Plumber Platform only</li>
    <li><strong class="text-foreground">CLI</strong>: Open Source CLI only (not a Platform control)</li>
  </ul>
</div>

<ProviderTabs />

<div data-provider-panel="gitlab">

## CI/CD Container Images
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="all" /> | Container images must come from authorized sources [ISSUE-101](/docs/use-plumber/issues/ISSUE-101?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that container images used to run your CI/CD pipelines come from authorized and trusted sources.</span> | <span style={{fontSize: '0.85em'}}>Helps mitigate security risks introduced by the use of malicious, compromised, or vulnerable images.</span> |
| <ProductScopeBadge scope="all" /> | Container images must not use forbidden tags [ISSUE-102](/docs/use-plumber/issues/ISSUE-102?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that container images used to run your CI/CD pipelines rely on authorized tags.</span> | <span style={{fontSize: '0.85em'}}>Helps mitigate both security and functional risks introduced by the use of unverified, outdated, or compromised image versions.</span> |
| <ProductScopeBadge scope="cli" /> | Container images must be pinned by digest [ISSUE-103](/docs/use-plumber/issues/ISSUE-103?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that container images are referenced by their SHA256 digest rather than a mutable tag.</span> | <span style={{fontSize: '0.85em'}}>Prevents supply chain attacks where a tag is overwritten with a compromised image, guaranteeing exact image content in every pipeline run.</span> |

## CI/CD Variables
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="platform" /> | CI/CD variables must be protected [ISSUE-201](/docs/use-plumber/issues/ISSUE-201?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that CI/CD variables used in a project have the protected field enabled.</span> | <span style={{fontSize: '0.85em'}}>Ensures sensitive values are restricted to protected branches or tags, reducing unauthorized exposure.</span> |
| <ProductScopeBadge scope="platform" /> | CI/CD variables must be masked [ISSUE-202](/docs/use-plumber/issues/ISSUE-202?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that CI/CD variables used in a project have the masked field enabled.</span> | <span style={{fontSize: '0.85em'}}>Prevents variable values from being exposed in pipeline logs, reducing the risk of leaks.</span> |
| <ProductScopeBadge scope="cli" /> | Pipeline must not enable debug trace [ISSUE-203](/docs/use-plumber/issues/ISSUE-203?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that <code style={{fontSize: '0.85em'}}>CI_DEBUG_TRACE</code> and <code style={{fontSize: '0.85em'}}>CI_DEBUG_SERVICES</code> are not enabled in the pipeline configuration.</span> | <span style={{fontSize: '0.85em'}}>Prevents exposure of all CI/CD variable values in job logs, including secrets and tokens.</span> |
| <ProductScopeBadge scope="cli" /> | Pipeline must not use unsafe variable expansion [ISSUE-204](/docs/use-plumber/issues/ISSUE-204?p=gitlab) | <span style={{fontSize: '0.85em'}}>Detects user-controlled CI variables expanded in shell re-interpretation contexts (<code style={{fontSize: '0.85em'}}>eval</code>, <code style={{fontSize: '0.85em'}}>sh -c</code>, <code style={{fontSize: '0.85em'}}>bash -c</code>, <code style={{fontSize: '0.85em'}}>source</code>).</span> | <span style={{fontSize: '0.85em'}}>Prevents command injection via crafted branch names, MR titles, or commit messages (OWASP CICD-SEC-1).</span> |
| <ProductScopeBadge scope="cli" /> | Pipeline must not override job variables [ISSUE-205](/docs/use-plumber/issues/ISSUE-205?p=gitlab) | <span style={{fontSize: '0.85em'}}>Detects controlled CI/CD variables redefined in <code style={{fontSize: '0.85em'}}>.gitlab-ci.yml</code> that should only be set in GitLab CI/CD Settings.</span> | <span style={{fontSize: '0.85em'}}>Reduces risk of tampering with scanner settings or other governed variables via pipeline YAML.</span> |

## CI/CD Secrets
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="cli" /> | Pipeline must not leak secrets in configuration [ISSUE-301](/docs/use-plumber/issues/ISSUE-301?p=gitlab) | <span style={{fontSize: '0.85em'}}>Pipes the resolved <code style={{fontSize: '0.85em'}}>.gitlab-ci.yml</code> (with all <code style={{fontSize: '0.85em'}}>include:</code>s merged) through <a href="https://gitleaks.io">gitleaks</a>. Any high-confidence match against the built-in or a custom rule set surfaces as a Critical finding with the secret value redacted to first/last 4 chars.</span> | <span style={{fontSize: '0.85em'}}>Catches the most common credential-leak path (someone pastes a token into the YAML "just to test"). Detection is opt-in because gitleaks must be installed separately; once enabled, redaction guarantees the raw secret never reaches the JSON report.</span> |

## Pipeline Composition
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="all" /> | Pipelines must include templates [ISSUE-405](/docs/use-plumber/issues/ISSUE-405?p=gitlab) [ISSUE-406](/docs/use-plumber/issues/ISSUE-406?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the projects contain specific templates. This control can also allow overriding certain variables in the included templates.</span> | <span style={{fontSize: '0.85em'}}>Ensures pipelines comply with required security and compliance practices.</span> |
| <ProductScopeBadge scope="all" /> | Pipelines must include components [ISSUE-408](/docs/use-plumber/issues/ISSUE-408?p=gitlab) [ISSUE-409](/docs/use-plumber/issues/ISSUE-409?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the projects contain specific GitLab components. This control can also allow overriding certain variables in the included components.</span> | <span style={{fontSize: '0.85em'}}>Ensures pipelines integrate mandatory security and compliance steps.</span> |
| <ProductScopeBadge scope="platform" /> | Pipeline must include required phases [ISSUE-407](/docs/use-plumber/issues/ISSUE-407?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the CI/CD pipeline includes a group of job types.</span> | <span style={{fontSize: '0.85em'}}>Ensures completeness and compliance of the pipeline execution flow.</span> |
| <ProductScopeBadge scope="all" /> | Pipeline must not contain hardcoded jobs [ISSUE-401](/docs/use-plumber/issues/ISSUE-401?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that no hardcoded job is used in CI/CD pipelines.</span> | <span style={{fontSize: '0.85em'}}>Improves maintainability and ensures compliance with best practices.</span> |
| <ProductScopeBadge scope="all" /> | Pipeline must not use forbidden ref in includes [ISSUE-404](/docs/use-plumber/issues/ISSUE-404?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the included refs are using specified tags.</span> | <span style={{fontSize: '0.85em'}}>Prevents reliance on insecure or non-compliant references.</span> |
| <ProductScopeBadge scope="all" /> | Pipeline must use only up-to-date includes [ISSUE-403](/docs/use-plumber/issues/ISSUE-403?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the included pipelines are up-to-date compared to their source.</span> | <span style={{fontSize: '0.85em'}}>Reduces risks from outdated or vulnerable templates.</span> |
| <ProductScopeBadge scope="cli" /> | Security jobs must not be weakened [ISSUE-410](/docs/use-plumber/issues/ISSUE-410?p=gitlab) | <span style={{fontSize: '0.85em'}}>Detects security scanning jobs (SAST, Secret Detection, Container Scanning, etc.) weakened by <code style={{fontSize: '0.85em'}}>allow_failure: true</code>, <code style={{fontSize: '0.85em'}}>rules:</code> overrides with <code style={{fontSize: '0.85em'}}>when: never</code> / <code style={{fontSize: '0.85em'}}>when: manual</code>, or <code style={{fontSize: '0.85em'}}>when: manual</code> at job level.</span> | <span style={{fontSize: '0.85em'}}>Prevents silently neutralized security scans that give a false sense of compliance (OWASP CICD-SEC-4).</span> |
| <ProductScopeBadge scope="cli" /> | Pipeline must not execute unverified scripts [ISSUE-411](/docs/use-plumber/issues/ISSUE-411?p=gitlab) | <span style={{fontSize: '0.85em'}}>Detects jobs that download and immediately execute scripts from the internet (<code style={{fontSize: '0.85em'}}>curl &#124; bash</code>, <code style={{fontSize: '0.85em'}}>wget &#124; sh</code>, download-then-execute) without integrity verification.</span> | <span style={{fontSize: '0.85em'}}>Prevents supply chain attacks where a compromised URL serves a modified script that exfiltrates secrets (OWASP CICD-SEC-3, CICD-SEC-8).</span> |
| <ProductScopeBadge scope="cli" /> | Pipeline must not use Docker-in-Docker [ISSUE-412](/docs/use-plumber/issues/ISSUE-412?p=gitlab) [ISSUE-413](/docs/use-plumber/issues/ISSUE-413?p=gitlab) | <span style={{fontSize: '0.85em'}}>Detects Docker-in-Docker (<code style={{fontSize: '0.85em'}}>docker:dind</code>) services and insecure daemon configuration (for example plaintext Docker API).</span> | <span style={{fontSize: '0.85em'}}>Reduces container escape and lateral movement risk on shared runners; prefer Kaniko or Buildah for image builds.</span> |

## Access and Authorization
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="all" /> | Branch must be protected [ISSUE-501](/docs/use-plumber/issues/ISSUE-501?p=gitlab) [ISSUE-505](/docs/use-plumber/issues/ISSUE-505?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the project configuration respects the protection, push, merge and owner approval on included branch names.</span> | <span style={{fontSize: '0.85em'}}>Prevents unauthorized modifications and enforces branch protection standards.</span> |
| <ProductScopeBadge scope="platform" /> | MR approval rules must have at least N approval required [ISSUE-502](/docs/use-plumber/issues/ISSUE-502?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the project merge request approval rules that cover all protected branches have a minimum number of approval requirements.</span> | <span style={{fontSize: '0.85em'}}>Prevents unreviewed code from being merged, reducing security risks.</span> |
| <ProductScopeBadge scope="platform" /> | MR approval settings must be compliant [ISSUE-503](/docs/use-plumber/issues/ISSUE-503?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that MR approval settings are properly configured.</span> | <span style={{fontSize: '0.85em'}}>Ensures compliance with review and security requirements.</span> |
| <ProductScopeBadge scope="platform" /> | An MR approval rule must be defined to cover all protected branches [ISSUE-504](/docs/use-plumber/issues/ISSUE-504?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the protected branches have at least one approval rule.</span> | <span style={{fontSize: '0.85em'}}>Ensures protected branches cannot bypass review processes.</span> |
| <ProductScopeBadge scope="platform" /> | MR settings must be compliant [ISSUE-506](/docs/use-plumber/issues/ISSUE-506?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the project's merge request settings are correct in terms of merge method, resolving differences, squashing, etc.</span> | <span style={{fontSize: '0.85em'}}>Reduces risk of unauthorized or insecure code changes.</span> |
| <ProductScopeBadge scope="platform" /> | Number of project members must respect a quota [ISSUE-507](/docs/use-plumber/issues/ISSUE-507?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the project configuration respects the owner, maintainer and developer quotas.</span> | <span style={{fontSize: '0.85em'}}>Prevents uncontrolled access that could weaken project security.</span> |
| <ProductScopeBadge scope="platform" /> | Number of group members must respect a quota [ISSUE-508](/docs/use-plumber/issues/ISSUE-508?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies that the group configuration respects the owner, maintainer and developer quotas.</span> | <span style={{fontSize: '0.85em'}}>Prevents uncontrolled access at group level, strengthening governance.</span> |

## Security Source
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="platform" /> | Project must have a security policy source [ISSUE-601](/docs/use-plumber/issues/ISSUE-601?p=gitlab) | <span style={{fontSize: '0.85em'}}>Verifies if the projects have a specific project as their source of security policy.</span> | <span style={{fontSize: '0.85em'}}>Ensures compliance with security policy and reduces risk of unmanaged vulnerabilities.</span> |

</div>

<div data-provider-panel="github" class="hidden">

<div class="not-content my-4 rounded-md border border-emerald-300/60 dark:border-emerald-700/40 bg-emerald-50/50 dark:bg-emerald-950/20 px-3 py-2.5 text-[13px] leading-snug text-foreground/80">
  <p class="mb-1 font-medium text-foreground">GitHub Actions support is currently <strong>Open Source CLI only</strong>.</p>
  <p class="text-muted-foreground">All controls below carry the <ProductScopeBadge scope="cli" /> badge. Plumber Platform does not scan GitHub Actions workflows yet; Platform GitHub support follows in a later release.</p>
</div>

<div class="not-content my-4 rounded-md border border-border bg-muted/30 px-3 py-2.5 text-[13px] leading-snug text-muted-foreground">
  <p class="mb-2 font-medium text-foreground">What Plumber scans (GitHub CLI)</p>
  <ul class="m-0 list-disc space-y-1 pl-4">
    <li><strong class="text-foreground">Source:</strong> committed files under <code style={{fontSize: '0.85em'}}>.github/workflows/</code> in the analyzed ref (local clone or remote fetch).</li>
    <li><strong class="text-foreground">Static YAML only:</strong> no org/repo Variables, no runtime <code style={{fontSize: '0.85em'}}>$GITHUB_ENV</code> writes, no evaluation of GitHub expression syntax in values.</li>
    <li><strong class="text-foreground">Step <code style={{fontSize: '0.85em'}}>uses:</code></strong> third-party <code style={{fontSize: '0.85em'}}>owner/repo@ref</code> actions (ISSUE-702/703). Not local <code style={{fontSize: '0.85em'}}>./.github/actions/*</code>, not job-level reusable-workflow <code style={{fontSize: '0.85em'}}>uses:</code>, not nested actions inside composites.</li>
    <li><strong class="text-foreground">API-backed rules (ISSUE-702, ISSUE-703):</strong> need <code style={{fontSize: '0.85em'}}>gh auth login</code> or <code style={{fontSize: '0.85em'}}>GH_TOKEN</code>. Without auth they <strong class="text-foreground">abstain</strong> (no finding, not a pass).</li>
  </ul>
</div>

## CI/CD Container Images
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="all" /> | Container images must not use forbidden tags [ISSUE-102](/docs/use-plumber/issues/ISSUE-102?p=github) | <span style={{fontSize: '0.85em'}}>Same forbidden-tag policy (<code style={{fontSize: '0.85em'}}>latest</code>, <code style={{fontSize: '0.85em'}}>dev</code>, …), applied to GitHub Actions container references. With the embedded option <code style={{fontSize: '0.85em'}}>containerImagesMustBePinnedByDigest: true</code>, any non-digest reference also fails.</span> | <span style={{fontSize: '0.85em'}}>Mutable tags create non-reproducible builds and a path for surprise upgrades.</span> |

## CI/CD Variables
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="cli" /> | Pipeline must not enable debug trace [ISSUE-203](/docs/use-plumber/issues/ISSUE-203?p=github) | <span style={{fontSize: '0.85em'}}>Static <code style={{fontSize: '0.85em'}}>env:</code> (workflow/job/step, merged per job): truthy literals plus GitHub expressions on forbidden names. Also <code style={{fontSize: '0.85em'}}>run:</code> lines that write forbidden names to <code style={{fontSize: '0.85em'}}>$GITHUB_ENV</code>.</span> | <span style={{fontSize: '0.85em'}}>Debug toggles dump masked secrets into logs. Org/repo Variables with no YAML reference and UI "Re-run with debug logging" remain out of scope.</span> |
| <ProductScopeBadge scope="cli" /> | Workflow must not inline user input into shell scripts [ISSUE-207](/docs/use-plumber/issues/ISSUE-207?p=github) | <span style={{fontSize: '0.85em'}}>Detects <code style={{fontSize: '0.85em'}}>$&#123;&#123; github.event.&#42; &#125;&#125;</code> expressions inlined directly into <code style={{fontSize: '0.85em'}}>run:</code> shell scripts.</span> | <span style={{fontSize: '0.85em'}}>Workflow template-injection is the GitHub Actions equivalent of SQL injection: the PR title or branch name becomes part of the executed shell command.</span> |

## CI/CD Secrets
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="cli" /> | Reusable workflow must not use `secrets: inherit` [ISSUE-302](/docs/use-plumber/issues/ISSUE-302?p=github) | <span style={{fontSize: '0.85em'}}>Flags callers that pass <code style={{fontSize: '0.85em'}}>secrets: inherit</code> instead of declaring the secrets the callee needs.</span> | <span style={{fontSize: '0.85em'}}>Inheriting hands every caller secret to the reusable workflow — a third-party reusable workflow then sees secrets it should never see.</span> |

## Pipeline Composition
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="cli" /> | Security jobs must not be weakened [ISSUE-410](/docs/use-plumber/issues/ISSUE-410?p=github) | <span style={{fontSize: '0.85em'}}>Detects security scanning jobs neutralized by <code style={{fontSize: '0.85em'}}>continue-on-error: true</code>, narrow <code style={{fontSize: '0.85em'}}>if:</code> conditions, or always-skip triggers.</span> | <span style={{fontSize: '0.85em'}}>Same OWASP CICD-SEC-4 pattern as on GitLab — a green pipeline that secretly skipped its security scan is worse than no scan.</span> |
| <ProductScopeBadge scope="cli" /> | Workflow must not use Docker-in-Docker [ISSUE-412](/docs/use-plumber/issues/ISSUE-412?p=github) [ISSUE-413](/docs/use-plumber/issues/ISSUE-413?p=github) | <span style={{fontSize: '0.85em'}}>Detects DinD service usage and insecure daemon configuration in workflow jobs.</span> | <span style={{fontSize: '0.85em'}}>Container escape and lateral movement risk applies to self-hosted runners the same way it does to GitLab shared runners.</span> |
| <ProductScopeBadge scope="cli" /> | Workflows must include required actions [ISSUE-417](/docs/use-plumber/issues/ISSUE-417?p=github) | <span style={{fontSize: '0.85em'}}>Asserts that every workflow file under <code style={{fontSize: '0.85em'}}>.github/workflows/</code> collectively references a configured set of required actions or reusable workflows. Step-level <code style={{fontSize: '0.85em'}}>uses:</code> and job-level reusable-workflow <code style={{fontSize: '0.85em'}}>uses:</code> both count. Matching is ref-agnostic with a slash-guard against accidental prefix collisions.</span> | <span style={{fontSize: '0.85em'}}>Closes the gap between an org-wide policy that says "every repo runs the security suite" and a per-repo workflow that quietly dropped the <code style={{fontSize: '0.85em'}}>uses:</code> line. Disabled by default; opt in once the required action set is settled.</span> |

## Access and Authorization
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="all" /> | Branch must be protected [ISSUE-501](/docs/use-plumber/issues/ISSUE-501?p=github) [ISSUE-505](/docs/use-plumber/issues/ISSUE-505?p=github) | <span style={{fontSize: '0.85em'}}>Reads both classic Branch Protection and any Repository or Organization Ruleset covering the branch. Verifies required reviews, force-push prevention, code-owner approval, and status checks against the merged effective configuration (stricter wins).</span> | <span style={{fontSize: '0.85em'}}>Prevents direct push and force-push attacks on protected refs even when half the protection lives in classic Branch Protection and the other half in a Ruleset.</span> |

## Third-party actions
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="cli" /> | Third-party actions must be pinned by commit SHA [ISSUE-701](/docs/use-plumber/issues/ISSUE-701?p=github) | <span style={{fontSize: '0.85em'}}>Verifies every <code style={{fontSize: '0.85em'}}>uses: owner/repo@ref</code> is pinned by a 40-character commit SHA, not a mutable tag or branch.</span> | <span style={{fontSize: '0.85em'}}>Defends against the March 2025 tj-actions / reviewdog supply chain compromise pattern (CVE-2025-30066), where attackers retag actions to point at malicious code that then runs with the caller's secrets.</span> |
| <ProductScopeBadge scope="cli" /> | Actions must not reference archived repositories [ISSUE-702](/docs/use-plumber/issues/ISSUE-702?p=github) | <span style={{fontSize: '0.85em'}}>Step-level <code style={{fontSize: '0.85em'}}>uses: owner/repo@ref</code> in committed workflows. Queries GitHub for <code style={{fontSize: '0.85em'}}>archived: true</code> (one cached API call per <code style={{fontSize: '0.85em'}}>owner/repo</code>). Requires <code style={{fontSize: '0.85em'}}>gh</code> / <code style={{fontSize: '0.85em'}}>GH_TOKEN</code>; abstains without auth.</span> | <span style={{fontSize: '0.85em'}}>Archived repos stop receiving patches. Does not inspect reusable-workflow callees, local actions, or runtime installs. PBOM: <code style={{fontSize: '0.85em'}}>archived: true</code>.</span> |
| <ProductScopeBadge scope="cli" /> | Actions must not carry known CVEs [ISSUE-703](/docs/use-plumber/issues/ISSUE-703?p=github) | <span style={{fontSize: '0.85em'}}>Same step-level <code style={{fontSize: '0.85em'}}>uses:</code> scope as ISSUE-702. Queries GitHub Advisory Database (<code style={{fontSize: '0.85em'}}>actions</code> ecosystem); semver-filters by <code style={{fontSize: '0.85em'}}>vulnerable_version_range</code> when the ref is a tag. SHA pins without a resolvable tag may match conservatively.</span> | <span style={{fontSize: '0.85em'}}>Catches known-vulnerable action versions (e.g. tj-actions CVE-2025-30066). Requires API auth; abstains without it. PBOM: <code style={{fontSize: '0.85em'}}>hasCve</code> + <code style={{fontSize: '0.85em'}}>advisories: [GHSA-…]</code>.</span> |

## Workflow triggers and permissions
| Scope | Control | What it checks | Why it matters |
|-------|---------|----------------|---------------|
| <ProductScopeBadge scope="cli" /> | Workflow permissions must be declared [ISSUE-801](/docs/use-plumber/issues/ISSUE-801?p=github) | <span style={{fontSize: '0.85em'}}>Flags workflows that omit a top-level <code style={{fontSize: '0.85em'}}>permissions:</code> block, relying on the repo's default GITHUB_TOKEN permissions.</span> | <span style={{fontSize: '0.85em'}}>Implicit permissions default to whatever the repo / org allows, often write-all. Explicit minimum scopes shrink blast radius on a token leak.</span> |
| <ProductScopeBadge scope="cli" /> | Workflow must not subscribe to dangerous triggers [ISSUE-802](/docs/use-plumber/issues/ISSUE-802?p=github) | <span style={{fontSize: '0.85em'}}>Flags workflows triggered by <code style={{fontSize: '0.85em'}}>pull_request_target</code> or <code style={{fontSize: '0.85em'}}>workflow_run</code>.</span> | <span style={{fontSize: '0.85em'}}>These triggers grant secret access to PR-author-controlled code paths. They are the root cause behind the highest-impact GitHub Actions CVEs of the last two years.</span> |
| <ProductScopeBadge scope="cli" /> | Workflow must not grant write-all permissions [ISSUE-803](/docs/use-plumber/issues/ISSUE-803?p=github) | <span style={{fontSize: '0.85em'}}>Static <code style={{fontSize: '0.85em'}}>permissions:</code> in workflow YAML. Flags only the literal shortcut <code style={{fontSize: '0.85em'}}>write-all</code> (workflow-level propagates to every job). Not <code style={{fontSize: '0.85em'}}>read-all</code>, not scope maps like <code style={{fontSize: '0.85em'}}>contents: write</code>, not missing blocks (ISSUE-801).</span> | <span style={{fontSize: '0.85em'}}><code style={{fontSize: '0.85em'}}>write-all</code> grants every <code style={{fontSize: '0.85em'}}>GITHUB_TOKEN</code> scope at once. Pair with ISSUE-801 for jobs with no explicit block.</span> |

</div>

<ProviderTabsScript />
